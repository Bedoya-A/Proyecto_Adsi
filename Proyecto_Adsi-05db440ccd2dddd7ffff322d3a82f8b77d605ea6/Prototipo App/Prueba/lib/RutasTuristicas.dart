import 'package:flutter/material.dart';

class RutasTuristicas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Rutas Turísticas')),
      body: Center(
        child: Text('Información sobre las Rutas Turísticas'),
      ),
    );
  }
}
