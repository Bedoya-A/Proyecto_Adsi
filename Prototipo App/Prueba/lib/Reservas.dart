import 'package:flutter/material.dart';

class Reservas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Reservas')),
      body: Center(
        child: Text('Información sobre Reservas'),
      ),
    );
  }
}
